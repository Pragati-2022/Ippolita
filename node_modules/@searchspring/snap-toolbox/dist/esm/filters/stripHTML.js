export function stripHTML(input) {
    if (typeof input != 'string') {
        return input;
    }
    return input
        .replace(/<(?:.|\n)*?>/gm, ' ')
        .replace(/\s+/g, ' ')
        .trim();
}
